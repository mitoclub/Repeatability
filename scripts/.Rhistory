major_arc_end <- 16568
rep <- read.table(
"../data/2_derived/01KP/01KP.8473.T.txt",
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end   == rep$repeat.end)
motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & motif_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
max_len <- max(rep1$effective.length, na.rm = TRUE)
rep_max <- rep1[rep1$effective.length == max_len, ]
View(rep_max)
#major_arc_start <- 5798
#major_arc_end <- 16568
rep <- read.table(
"../data/2_derived/01KP/01KP.8473.T.txt",
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end   == rep$repeat.end)
#motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
#repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
max_len <- max(rep1$effective.length, na.rm = TRUE)
rep_max <- rep1[rep1$effective.length == max_len, ]
View(rep_max)
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecInWholeMtDna.txt"
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecInWholeMtDna.txt"
#major_arc_start <- 5798
#major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
# Container for results
all_results <- list()
for (f in files) {
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
#motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
#repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Add filename info (optional but useful)
rep_max$file <- basename(f)
# Store in list
all_results[[length(all_results) + 1]] <- rep_max
}
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecInWholeMtDna.txt"
#major_arc_start <- 5798
#major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
n_files <- length(files)
# Container for results
all_results <- list()
# Simple counter
i <- 0
for (f in files) {
i <- i + 1
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
#motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
#repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
# Still show progress
cat(sprintf("Processed %d/%d files (%.1f%%): %s (no matches)\n",
i, n_files, 100 * i / n_files, basename(f)))
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Add filename info (optional but useful)
rep_max$file <- basename(f)
# Store in list
all_results[[length(all_results) + 1]] <- rep_max
# Progress message
cat(sprintf("Processed %d/%d files (%.1f%%): %s\n",
i, n_files, 100 * i / n_files, basename(f)))
}
# Concatenate all results into one data frame
if (length(all_results) > 0) {
final_df <- do.call(rbind, all_results)
# Write to output file
write.table(
final_df,
file = output_path,
quote = FALSE,
sep = "\t",
row.names = FALSE
)
} else {
message("No rows matched the filters in any file.")
}
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecInWholeMtDna.txt"
#major_arc_start <- 5798
#major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
n_files <- length(files)
# Container for results
all_results <- list()
# Simple counter
i <- 0
for (f in files) {
i <- i + 1
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
#motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
#repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
cat(sprintf("Processed %d/%d files (%.1f%%): %s (no matches)\n",
i, n_files, 100 * i / n_files, basename(f)))
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Add filename info (optional but useful)
rep_max$file <- basename(f)
# Store in list
all_results[[length(all_results) + 1]] <- rep_max
cat(sprintf("Processed %d/%d files (%.1f%%): %s\n",
i, n_files, 100 * i / n_files, basename(f)))
}
# Concatenate all results into one data frame
if (length(all_results) > 0) {
final_df <- do.call(rbind, all_results)
# Sort by pos (ascending)
final_df <- final_df[order(final_df$pos), ]
# Write to output file
write.table(
final_df,
file = output_path,
quote = FALSE,
sep = "\t",
row.names = FALSE
)
} else {
message("No rows matched the filters in any file.")
}
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecWithinMajorArc.txt"
major_arc_start <- 5798
major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
n_files <- length(files)
# Container for results
all_results <- list()
# Simple counter
i <- 0
for (f in files) {
i <- i + 1
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
cat(sprintf("Processed %d/%d files (%.1f%%): %s (no matches)\n",
i, n_files, 100 * i / n_files, basename(f)))
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Add filename info (optional but useful)
rep_max$file <- basename(f)
# Store in list
all_results[[length(all_results) + 1]] <- rep_max
cat(sprintf("Processed %d/%d files (%.1f%%): %s\n",
i, n_files, 100 * i / n_files, basename(f)))
}
# Concatenate all results into one data frame
if (length(all_results) > 0) {
final_df <- do.call(rbind, all_results)
# Sort by pos (ascending)
final_df <- final_df[order(final_df$pos), ]
# Write to output file
write.table(
final_df,
file = output_path,
quote = FALSE,
sep = "\t",
row.names = FALSE
)
} else {
message("No rows matched the filters in any file.")
}
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls(all=TRUE))
library(knitr)
# library(tidyverse)
# library(kableExtra)
library(dplyr)
library(tidyr)
library(ggplot2)
library(Biostrings)
library(stringdist) # install.packages("stringdist")
library(IRanges)
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecInWholeMtDna.txt"
#major_arc_start <- 5798
#major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
n_files <- length(files)
# Container for results
all_results <- list()
# Simple counter
i <- 0
for (f in files) {
i <- i + 1
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
#motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
#repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
# idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
cat(sprintf("Processed %d/%d files (%.1f%%): %s (no matches)\n",
i, n_files, 100 * i / n_files, basename(f)))
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Store in list (no extra columns like 'file')
all_results[[length(all_results) + 1]] <- rep_max
cat(sprintf("Processed %d/%d files (%.1f%%): %s\n",
i, n_files, 100 * i / n_files, basename(f)))
}
# Concatenate all results into one data frame
if (length(all_results) > 0) {
final_df <- do.call(rbind, all_results)
# Remove duplicated rows (keep one copy of each unique row)
final_df <- unique(final_df)
# Sort by pos (ascending)
final_df <- final_df[order(final_df$pos), ]
# Write to output file
write.table(
final_df,
file = output_path,
quote = FALSE,
sep = "\t",
row.names = FALSE
)
} else {
message("No rows matched the filters in any file.")
}
# Paths
input_dir  <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
output_path <- "/home/popa/Repeatability/data/2_derived/01C.TheLongestPerfecWithinMajorArc.txt"
major_arc_start <- 5798
major_arc_end <- 16568
# List all .txt files in the folder
files <- list.files(input_dir, pattern = "\\.txt$", full.names = TRUE)
n_files <- length(files)
# Container for results
all_results <- list()
# Simple counter
i <- 0
for (f in files) {
i <- i + 1
# Read current file
rep <- read.table(
f,
header = TRUE,
colClasses = c(
pos = "integer",
nuc = "character",
motif.seq = "character",
motif.length = "integer",
motif.start = "integer",
motif.end = "integer",
repeat.seq = "character",
repeat.start = "integer",
repeat.end = "integer",
repeat.hamming.distance = "integer",
effective.length = "integer"
)
)
# Build logical conditions step by step
motif_not_repeat <- !(rep$motif.start == rep$repeat.start & rep$motif.end == rep$repeat.end)
motif_in_major_arc <- rep$motif.start >= major_arc_start & rep$motif.end   <= major_arc_end
repeat_in_major_arc <- rep$repeat.start >= major_arc_start & rep$repeat.end   <= major_arc_end
perfect_similarity <- rep$repeat.hamming.distance == 0
pos_strictly_inside_motif <- rep$motif.start < rep$pos & rep$pos < rep$motif.end
idx <- motif_not_repeat & motif_in_major_arc & repeat_in_major_arc & perfect_similarity & pos_strictly_inside_motif # Combined index
# idx <- motif_not_repeat & perfect_similarity & pos_strictly_inside_motif # Combined index
filtered_rep <- rep[idx, ] # Filter rows
# Skip files with no matching rows
if (nrow(filtered_rep) == 0) {
cat(sprintf("Processed %d/%d files (%.1f%%): %s (no matches)\n",
i, n_files, 100 * i / n_files, basename(f)))
next
}
# Find maximal effective.length and keep all rows with that length
max_len <- max(filtered_rep$effective.length, na.rm = TRUE)
rep_max <- filtered_rep[filtered_rep$effective.length == max_len, ]
# Store in list (no extra columns like 'file')
all_results[[length(all_results) + 1]] <- rep_max
cat(sprintf("Processed %d/%d files (%.1f%%): %s\n",
i, n_files, 100 * i / n_files, basename(f)))
}
# Concatenate all results into one data frame
if (length(all_results) > 0) {
final_df <- do.call(rbind, all_results)
# Remove duplicated rows (keep one copy of each unique row)
final_df <- unique(final_df)
# Sort by pos (ascending)
final_df <- final_df[order(final_df$pos), ]
# Write to output file
write.table(
final_df,
file = output_path,
quote = FALSE,
sep = "\t",
row.names = FALSE
)
} else {
message("No rows matched the filters in any file.")
}
knitr::opts_chunk$set(echo = TRUE)
rm(list=ls(all=TRUE))
library(knitr)
# library(tidyverse)
# library(kableExtra)
library(dplyr)
library(tidyr)
library(ggplot2)
library(Biostrings)
library(stringdist) # install.packages("stringdist")
library(IRanges)
output_dir <- "/home/popa/Repeatability/data/2_derived/01KP.RESTORED"
# List all .txt files (non-recursive, adjust if you need subfolders)
txt_files <- list.files(output_dir, pattern = "\\.txt$", full.names = TRUE)
# Remove them
if (length(txt_files) > 0) {
file.remove(txt_files)
cat("Deleted", length(txt_files), "files.\n")
} else {
cat("No .txt files found.\n")
}
